# ════════════════════════════════════════════════════════════════════════════
# ColtraDataAi Requirements — Python 3.14 (Streamlit Cloud default)
# ════════════════════════════════════════════════════════════════════════════
# NOTE: Streamlit Cloud provisions the Python venv before cloning the repo,
# so runtime.txt / .python-version are read too late to change the Python
# version. All C-extension packages must have cp314 pre-built wheels.
# ════════════════════════════════════════════════════════════════════════════

# Core Framework (pure-Python wheel — pin OK)
streamlit==1.40.2

# HTTP & Networking (pure-Python — pin OK)
requests==2.31.0

# Data Processing — C extensions, need cp314 wheels.
# Lower bounds set to the first release with Python 3.13/3.14 wheel support.
pandas>=2.2.3
numpy>=2.2.0
scipy>=1.15.0

# File I/O (pure-Python — pin OK)
xlrd==2.0.1
openpyxl==3.1.5
xlsxwriter==3.2.0

# Visualization — C extensions, same wheel constraint.
matplotlib>=3.10.0
plotly==5.24.1
Pillow>=11.0.0
kaleido==0.2.1

# Reporting & Export (pure-Python — range OK)
reportlab>=4.0.0,<4.2

# AI/ML (pure-Python — pin OK)
anthropic==0.42.0
