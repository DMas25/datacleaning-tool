# ════════════════════════════════════════════════════════════════════════════
# ColtraDataAi Requirements — Python 3.14 (Streamlit Cloud default)
# ════════════════════════════════════════════════════════════════════════════
# IMPORTANT:
# Must remain compatible with Python 3.14 (Streamlit Cloud default).
# Avoid packages without cp314 wheels — they fall back to source builds
# which fail on Streamlit Cloud (missing system headers / no build tools).
#
# Streamlit Cloud provisions the Python venv before cloning the repo,
# so runtime.txt / .python-version cannot override the Python version.
# ════════════════════════════════════════════════════════════════════════════

# Core Framework (pure-Python wheel — pin OK)
streamlit==1.40.2

# HTTP & Networking — urllib3>=2.0 silences RequestsDependencyWarning
requests>=2.31
urllib3>=2.0,<3

# Data Processing — C extensions, need cp314 wheels.
# Lower bounds set to the first release with Python 3.13/3.14 wheel support.
pandas>=2.2.3
numpy>=2.2.0
scipy>=1.15.0

# File I/O (pure-Python — pin OK)
xlrd==2.0.1
openpyxl==3.1.5
xlsxwriter==3.2.0

# Visualization — C extensions, same wheel constraint.
matplotlib>=3.10.0
plotly==5.24.1
Pillow>=11.0.0
kaleido==0.2.1

# Reporting & Export (pure-Python — range OK)
reportlab>=4.0.0,<4.2

# AI/ML (pure-Python — pin OK)
anthropic==0.42.0
