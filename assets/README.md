Place the selected ColtraDataAi Version B logo here as:

- logo.png
- favicon.png

Preferred: transparent PNG or SVG exported from the original logo artwork.

The app will automatically use `logo.png` in the header and `favicon.png` as the browser tab icon when these files are present.
